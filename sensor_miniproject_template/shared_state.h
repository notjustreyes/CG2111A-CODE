#pragma once

#include "packets.h"

extern volatile TState buttonState;
extern volatile bool stateChanged;

extern volatile int move_state;
extern volatile int change_speed_state;
extern volatile int speed;